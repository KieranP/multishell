# Why it is like this, and the rules that follow

Telegraphic notes, one file per subject. Newest at the bottom within each. A
file here is binding, not background.

| Read                                                       | For                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ---------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [design/architecture.md](design/architecture.md)           | Core versus GUI, reconcile don't command, identity is the path, shelling out to git and which git, why nothing blocks a thread bar the one-second directory wait, detection scanning the PATH off the main thread, a child ended by its stopper and never by a task's cancel.                                                                                                                                                                                                                                                                                               |
| [design/state-and-store.md](design/state-and-store.md)     | Why persisted state is repaired rather than trusted, why projects are strict where the rest are lossy, why invariants are tested at random, when a file is moved aside, a project looked up per render and never held, saves off the main actor and in order, one copy of a build at a time, a retired agent's id forgotten on load, a renamed field keeping the key it was saved under.                                                                                                                                                                                    |
| [design/settings.md](design/settings.md)                   | Project over global, what blank means, a repository's file filling only gaps, the trust that guards a hook or path someone else committed, the checkout those paths are confined to, what export keeps, help behind an (i) of 200 characters at most, a settings page split rather than scrolled, the banner toggles.                                                                                                                                                                                                                                                       |
| [design/hooks.md](design/hooks.md)                         | Only a pre hook refuses, why a hook is sh inside a login and interactive shell with no history file, how one is stopped, why a new worktree gets files by a list instead, which list entries are refused or named, and where each hook runs.                                                                                                                                                                                                                                                                                                                                |
| [design/worktrees.md](design/worktrees.md)                 | What is watched and what is polled, naming, the trunk row and row ordering, removal to the Trash or outright, a missing directory, a stage stopped with its worktree, the NUL-terminated list, a checkout with no timeout but a Cancel, no badge on a tree still being built, a branch name refused before git runs, bare repositories, runtime state dropped in one place, the watcher off the main actor, a slow status asked less often, the line-count badge and the pool thread a dead mount still holds, the sidebar filter, a worktree's dot as its most urgent tab. |
| [design/merged-branch.md](design/merged-branch.md)         | How "this branch landed" is decided from three signs, none of which writes; reads paced by cost; why the user's git config cannot change a read; refnames, never bare names; one width of eight across projects.                                                                                                                                                                                                                                                                                                                                                            |
| [design/tabs-and-groups.md](design/tabs-and-groups.md)     | Pane trees, what a drag to another worktree does, groups beside each other, a drag nothing takes put back, no drag leaving the app, `activeTab` versus `shownTabs`, the drag with no image, a strip out of room, a wheel scrolling it sideways, a dropped tab taking the keyboard, the plus as a menu per agent.                                                                                                                                                                                                                                                            |
| [design/terminals.md](design/terminals.md)                 | Where a session's state comes from, how a shell ends, injected integration, click-to-move, dropped files in the order a drag names them and one quoting for every shell, agent and shell ids, the three layers a user's Ghostty config sits in the middle of, the temp file it reaches libghostty by and the allow list it is read through, bash's resident relay, the find bar, one banner per pane, one engine named outright.                                                                                                                                            |
| [design/agents.md](design/agents.md)                       | What the socket accepts, each agent's hooks and why each event was narrowed, a settings file's numbers kept as written, a retired agent forgotten on load, the board as a roster, flags per agent against one list of placeholders, quoted for any shell, subagents as a roster shown as a chip, the selected worktree's pane rows, each agent's drawn mark, an agent typed at a prompt known by its command, and backgrounded shells counted as workers.                                                                                                                   |
| [design/appearance.md](design/appearance.md)               | Hex themes, hand-drawn window chrome, the header and row heights, one workspace window, the focused pane's ring and the fade, the find bar's look, a board card's two lines, project icons, sidebar rows that compare equal, the macOS 26 control style.                                                                                                                                                                                                                                                                                                                    |
| [design/translation.md](design/translation.md)             | Every word a lookup by key written at the call site, why a view holds no literal, a catalogue per frontend and one for the libraries, counted forms, what stays in English, an error's words kept off its type, no language picker, the English a library holds that the literal scan cannot see.                                                                                                                                                                                                                                                                           |
| [design/signing.md](design/signing.md)                     | Why a local build carries a dev certificate rather than signing ad hoc and what happens without one, the hardened runtime, the entitlements a pane needs, and `get-task-allow` on a debug build.                                                                                                                                                                                                                                                                                                                                                                            |
| [design/smaller-decisions.md](design/smaller-decisions.md) | What has no other file to go in: the one script run as root, the keys that answer a dialog, why a removal confirms in an `NSAlert`, and the Escape set by hand on the quit alert and every destructive alert's Cancel.                                                                                                                                                                                                                                                                                                                                                      |
