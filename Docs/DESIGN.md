# Why it is like this, and the rules that follow

Telegraphic notes, one file per subject. Newest at the bottom within each. A
file here is binding, not background.

| Read                                                       | For                                                                                                                                                                                                                                                                                                                                                  |
| ---------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [design/architecture.md](design/architecture.md)           | Core versus GUI, reconcile don't command, identity is the path, shelling out to git, why nothing blocks a thread.                                                                                                                                                                                                                                    |
| [design/state-and-store.md](design/state-and-store.md)     | Why persisted state is repaired rather than trusted, why projects are strict where the rest are lossy, why invariants are tested at random, when a file is moved aside, saves off the main actor and in order, one copy of a build at a time.                                                                                                        |
| [design/settings.md](design/settings.md)                   | Project over global, a repository's file filling only gaps, what blank means, and the trust that guards a hook someone else committed.                                                                                                                                                                                                               |
| [design/hooks.md](design/hooks.md)                         | Why a hook is a login and interactive shell, only a pre hook refuses, how one is stopped, and why a new worktree gets files by a list instead.                                                                                                                                                                                                       |
| [design/worktrees.md](design/worktrees.md)                 | What is watched and what is polled, naming, the trunk row and row ordering, removal to the Trash, a branch name refused before git runs, bare repositories, a missing directory, the NUL-terminated list, runtime state dropped in one place and its stage stopped with it, a checkout with no timeout but a Cancel, a slow status asked less often. |
| [design/merged-branch.md](design/merged-branch.md)         | How "this branch landed" is decided from three signs, none of which writes; why the user's git config cannot change a read; refnames, never bare names.                                                                                                                                                                                              |
| [design/tabs-and-columns.md](design/tabs-and-columns.md)   | Pane trees, what a drag to another worktree does, columns beside each other, `activeTab` versus `shownTabs`, the drag with no image, a strip out of room, a dropped tab taking the keyboard.                                                                                                                                                         |
| [design/terminals.md](design/terminals.md)                 | Where a session's state comes from, how a shell ends, injected integration, click-to-move, dropped files, agent and shell ids, the three layers a user's Ghostty config sits in the middle of, the allow list it is read through, and the temp file it reaches libghostty by.                                                                        |
| [design/agents.md](design/agents.md)                       | What the socket accepts, each agent's hooks and why each event was narrowed, a settings file's numbers kept as written, the board as a roster, subagents as a roster by id shown as a chip, the selected worktree's pane rows, and flags per agent against one list of placeholders.                                                                 |
| [design/appearance.md](design/appearance.md)               | Hex themes, the focused pane's ring and the fade, hand-drawn window chrome, one workspace window, the header and row heights, a board card's three lines.                                                                                                                                                                                            |
| [design/translation.md](design/translation.md)             | A catalogue per frontend and one for the libraries, why a view holds no literal, counted forms, what stays in English, no language picker.                                                                                                                                                                                                           |
| [design/signing.md](design/signing.md)                     | Why a local build carries a dev certificate rather than signing ad hoc, and what happens without one.                                                                                                                                                                                                                                                |
| [design/smaller-decisions.md](design/smaller-decisions.md) | What has no other file to go in: today, the one script run as root.                                                                                                                                                                                                                                                                                  |
