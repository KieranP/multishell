# Why it is like this, and the rules that follow

Telegraphic notes, one file per subject. Newest at the bottom within each. A
file here is binding, not background.

| Read                                                       | For                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ---------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [design/architecture.md](design/architecture.md)           | Core versus GUI, reconcile don't command, identity is the path, shelling out to git and which git, why nothing blocks a thread bar the one-second directory wait, detection scanning the PATH off the main thread, a child ended by its stopper and never by a task's cancel.                                                                                                                                                                                       |
| [design/state-and-store.md](design/state-and-store.md)     | Why persisted state is repaired rather than trusted, why projects are strict where the rest are lossy, a project looked up per render and never held, why invariants are tested at random, when a file is moved aside, saves off the main actor and in order, one copy of a build at a time, a retired agent's id forgotten on load.                                                                                                                                |
| [design/settings.md](design/settings.md)                   | Project over global, a repository's file filling only gaps, what blank means, the trust that guards a hook or path someone else committed, the checkout those paths are confined to, what export keeps, a settings page split rather than scrolled, the banner toggles.                                                                                                                                                                                             |
| [design/hooks.md](design/hooks.md)                         | Why a hook is a login and interactive shell with no history file, only a pre hook refuses, how one is stopped, where each runs, why a new worktree gets files by a list instead, and which list entries are refused or named.                                                                                                                                                                                                                                       |
| [design/worktrees.md](design/worktrees.md)                 | What is watched and what is polled, the watcher off the main actor, naming, the trunk row and row ordering, removal to the Trash or outright, a branch name refused before git runs, bare repositories, a missing directory, the NUL-terminated list, runtime state dropped in one place and its stage stopped with it, a checkout with no timeout but a Cancel, a slow status asked less often, the line-count badge and the pool thread a dead mount still holds. |
| [design/merged-branch.md](design/merged-branch.md)         | How "this branch landed" is decided from three signs, none of which writes; why the user's git config cannot change a read; refnames, never bare names; reads paced by cost.                                                                                                                                                                                                                                                                                        |
| [design/tabs-and-columns.md](design/tabs-and-columns.md)   | Pane trees, what a drag to another worktree does, columns beside each other, `activeTab` versus `shownTabs`, the drag with no image, a drag nothing takes put back, no drag leaving the app, a strip out of room, a wheel scrolling it sideways, the plus as a menu per agent, a dropped tab taking the keyboard.                                                                                                                                                   |
| [design/terminals.md](design/terminals.md)                 | Where a session's state comes from, how a shell ends, injected integration, bash's resident relay, click-to-move, dropped files and one quoting for every shell, agent and shell ids, the find bar, one banner per pane, the three layers a user's Ghostty config sits in the middle of, the allow list it is read through, and the temp file it reaches libghostty by.                                                                                             |
| [design/agents.md](design/agents.md)                       | What the socket accepts, each agent's hooks and why each event was narrowed, a settings file's numbers kept as written, a retired agent forgotten on load, the board as a roster, subagents and backgrounded shells as a roster shown as a chip, the selected worktree's pane rows, each agent's drawn mark, and flags per agent against one list of placeholders, quoted for any shell.                                                                            |
| [design/appearance.md](design/appearance.md)               | Hex themes, the focused pane's ring and the fade, hand-drawn window chrome, one workspace window, the header and row heights, the find bar's look, a board card's two lines, project icons, sidebar rows that compare equal, the macOS 26 control style.                                                                                                                                                                                                            |
| [design/translation.md](design/translation.md)             | A catalogue per frontend and one for the libraries, why a view holds no literal, counted forms, what stays in English, no language picker.                                                                                                                                                                                                                                                                                                                          |
| [design/signing.md](design/signing.md)                     | Why a local build carries a dev certificate rather than signing ad hoc and what happens without one, the hardened runtime, and the entitlements a pane needs.                                                                                                                                                                                                                                                                                                       |
| [design/smaller-decisions.md](design/smaller-decisions.md) | What has no other file to go in: the one script run as root, the keys that answer a dialog, and why a removal confirms in an `NSAlert`.                                                                                                                                                                                                                                                                                                                             |
